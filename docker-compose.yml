
services:
  db:
    container_name: db
    image: postgres:17.5
    restart: always
    ports:
      - $DB_PORT:5432
    environment:
     - POSTGRES_USER=$DB_USER
     - POSTGRES_PASSWORD=$DB_PASS
     - POSTGRES_DB=$DB_NAME
